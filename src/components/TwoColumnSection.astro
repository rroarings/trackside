---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

interface Props {
  title: string;
  content?: string;
  description?: string;
  ctaText?: string;
  ctaLink?: string;
  imageSrc?: ImageMetadata;
  imageAlt?: string;
  reversed?: boolean;
  background?: "white" | "slate";
  imageLoading?: "eager" | "lazy";
}

const {
  title,
  content,
  description,
  ctaText,
  ctaLink,
  imageSrc,
  imageAlt = "",
  reversed = false,
  imageLoading = "lazy",
} = Astro.props;
---

<section class="bg-slate-100 py-12 dark:bg-slate-900">
  <div class="container mx-auto">
    <div
      class={`flex flex-col lg:flex-row gap-8 items-center ${reversed ? "lg:flex-row-reverse" : ""}`}
    >
      <!-- Text Content -->
      <div class={`flex-1 space-y-6 `}>
        <h2 class="font-bold dark:text-slate-100">{title}</h2>
        {
          content ? (
            <div
              class="prose prose-lg max-w-none text-slate-800 dark:text-slate-300"
              set:html={content}
            />
          ) : description ? (
            <p class="text-slate-600 dark:text-slate-300">{description}</p>
          ) : null
        }
        {
          ctaText && ctaLink && (
            <a
              href={ctaLink}
              class="inline-flex items-center rounded border-2 border-transparent bg-slate-600 px-4 py-2 text-white transition-all duration-300 hover:bg-slate-700 dark:bg-slate-700 dark:hover:border-slate-600 dark:hover:bg-slate-800"
            >
              <span>{ctaText}</span>
              <svg
                class="ml-2 h-5 w-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 5l7 7-7 7"
                />
              </svg>
            </a>
          )
        }
      </div>

      <!-- Image -->
      <div class={`flex-1 space-y-6 `}>
        {
          imageSrc ? (
            <Image
              src={imageSrc}
              alt={imageAlt}
              class="h-100 w-full max-w-2xl rounded-lg object-cover"
              loading={imageLoading}
            />
          ) : (
            <div class="flex h-[400px] w-full max-w-2xl items-center justify-center rounded-3xl border-4 border-white bg-slate-200 dark:border-slate-900">
              <span class="text-lg text-slate-400">[Placeholder Image]</span>
            </div>
          )
        }
      </div>
    </div>
  </div>
</section>
