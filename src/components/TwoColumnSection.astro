---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

interface Props {
  title: string;
  content?: string;
  description?: string;
  ctaText?: string;
  ctaLink?: string;
  imageSrc?: ImageMetadata;
  imageAlt?: string;
  reversed?: boolean;
  background?: "white" | "slate";
  imageLoading?: "eager" | "lazy";
}

const {
  title,
  content,
  description,
  ctaText,
  ctaLink,
  imageSrc,
  imageAlt = "",
  reversed = false,
  imageLoading = "lazy",
} = Astro.props;
---

<section class="overflow-x-hidden bg-slate-100 py-12 dark:bg-slate-900">
  <div class="mx-auto max-w-7xl px-4">
    <div
      class={`w-full flex flex-col md:flex-row lg:flex-row gap-8 items-center md:items-start ${reversed ? "md:flex-row-reverse lg:flex-row-reverse" : ""}`}
    >
      <!-- Text Content -->
      <div class={`flex-1 md:w-1/2 min-w-0 w-full space-y-6 `}>
        <h2 class="font-bold dark:text-slate-100">{title}</h2>
        {
          content ? (
            <div
              class="prose prose-lg wrap-break-word max-w-none whitespace-normal text-slate-800 dark:text-slate-300"
              set:html={content}
            />
          ) : description ? (
            <p class="text-slate-600 dark:text-slate-300">{description}</p>
          ) : null
        }
        {
          ctaText && ctaLink && (
            <a
              href={ctaLink}
              class="inline-flex items-center rounded border-2 border-transparent bg-slate-600 px-4 py-2 text-white transition-all duration-300 hover:bg-slate-700 dark:bg-slate-700 dark:hover:border-slate-600 dark:hover:bg-slate-800"
            >
              <span>{ctaText}</span>
              <svg
                class="ml-2 h-5 w-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 5l7 7-7 7"
                />
              </svg>
            </a>
          )
        }
      </div>

      <!-- Image -->
      <div class={`flex-1 md:flex-none md:w-1/2 min-w-0 w-full space-y-6 `}>
        {
          imageSrc ? (
            <Image
              src={imageSrc}
              alt={imageAlt}
              class="h-64 w-full max-w-2xl rounded-lg object-cover md:h-80 lg:h-96"
              loading={imageLoading}
            />
          ) : (
            <div class="flex h-96 w-full max-w-2xl items-center justify-center rounded-3xl border-4 border-white bg-slate-200 dark:border-slate-900">
              <span class="text-lg text-slate-400">[Placeholder Image]</span>
            </div>
          )
        }
      </div>
    </div>
  </div>
</section>
